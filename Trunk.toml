[build]
# The entry HTML template that trunk injects the WASM bundle into.
target = "index.html"
# Output directory for the built web bundle.
dist   = "dist"
# Use relative paths for itch.io CDN compatibility
public_url = "./"
release = true

# [[hooks]]
# stage = "pre_build"
# command = "mdbook"
# command_arguments = ["build", "docs"]
